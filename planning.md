Phase 1: Storage Tier Config & PyArrow Schema

~system-level workspace scaffolding: cross-platform hidden app directory follwing standard user convetions for linux/macOS/Windows

~PyArrow native schema: pyarrow primitive feild mappings, nomic-embed-text v1.5 8192 token cotext layout and supports hybrid retreival

~local database init: serverless, file-system bound storage connection pointing to app directory. mounts instantly and checks for schema regressions upon booting.
